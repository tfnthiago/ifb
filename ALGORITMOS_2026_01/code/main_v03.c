#include <stdio.h>

int main() {
    // Este é um comentário!
    int A = 10;
    int B = 20;
    int C = 30;
    // O %d faz referência a variáveis do tipo inteiro.
    printf("O valor da variavel <A> e: %d\n", A); 
    return 0;
}